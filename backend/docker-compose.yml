services:
  postgres:
    image: postgres:16-alpine
    container_name: velora-erp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: velora_erp
      POSTGRES_USER: velora
      POSTGRES_PASSWORD: velora
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: velora-erp-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
